apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ldapsync.fullname" . }}-test-connection"
  labels:
    {{- include "ldapsync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ldapsync.fullname" . }}']
  restartPolicy: Never
